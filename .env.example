# Optional: full AI analysis (https://console.anthropic.com/)
# VITE_ANTHROPIC_API_KEY=

# local = built-in rules only (default when key is missing)
# ai = always call Anthropic (requires key below)
# VITE_ANALYSIS_MODE=local
